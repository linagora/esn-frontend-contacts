.card
  .card-body
    .listview.lv-bordered
      .lv-body
        .lv-item
          .flex-space-between.flex-vertical-centered
            div(ng-if="$ctrl.domainId") {{ 'Synchronize domain members address book' | translate }}
            div(ng-if="!$ctrl.domainId") {{ 'Synchronize domain members address books for entire platform' | translate }}
            div
              button.btn.btn-primary(ng-click="$ctrl.onSyncBtnClick()") {{ 'Run' | translate }}